"""Initialize the core package for AJAX.

This file exists so that Python treats the `core` directory as a
package.  It defines nothing but allows sibling modules to be imported
using the `core` namespace (e.g. `from core import ajax_ai`).
"""